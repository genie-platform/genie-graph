{
  "name": "genie-graph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ genie-platform/genie-graph-v2",
    "create-local": "graph create --node http://localhost:8020/ genie-platform/genie-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ genie-platform/genie-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 genie-platform/genie-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  }
}
